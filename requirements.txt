# PennyCore — runtime dependencies pinned to compatible ranges.
# Day 5+ adds: psycopg[binary]>=3.1, redis>=5, sqlalchemy>=2, alembic>=1.13.
# Day 5+ also adds: anthropic>=0.40, openai>=1.40 (LLM dispatch layer).

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
python-dotenv>=1.0,<2.0
